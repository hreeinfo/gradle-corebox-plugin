buildscript {
    repositories {
        mavenLocal()
        jcenter()
        mavenCentral()
    }
}

configure(allprojects) {
    group = "${project_group}"
    version = "${project_version}"
    repositories {
        mavenLocal()
        jcenter()
        mavenCentral()
    }
}

